using System;
using System.Collections.Generic;
using ConsoleCatchall.Console.Reconciliation.Records;
using ExcelLibrary;
using Interfaces;
using Interfaces.Constants;
using NUnit.Framework;

namespace ExcelIntegrationTests.Records
{
    // These are the tests for ExpectedIncomeRecord
    [TestFixture]
    public partial class ExcelRecordTests //(ExpectedIncomeRecord)
    {
        // All these tests have been moved to ConsoleCatchallTests, as they are not actually integration tests (due to mocking).
    }
}
